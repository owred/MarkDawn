// Внутри класса Answer напишите метод CompareNumbers, который на вход принимает два числа и выводит, какое число большее, а какое меньшее.

using System;

public class Answer {
    static void CompareNumbers(int firstNumber, int secondNumber)
    {
        // Введите свое решение ниже
        firstNumber = 5;
        secondNumber = 6;
        if (firstNumber > secondNumber)
        System.Console.WriteLine(«Большее число: » + firstNumber + «, меньшее число: » + secondNumber);
        else System.Console.WriteLine($»Большее число: {firstNumber}, меньшее число: {secondNumber}»);

    }
if (true)
{
    else
    
}